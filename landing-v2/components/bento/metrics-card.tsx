"use client"
import { useEffect, useState } from "react"
function SN({ target, label, delay = 0 }: { target: string; label: string; delay?: number }) { const [d, setD] = useState(target.replace(/[0-9]/g, "0")); useEffect(() => { const t = setTimeout(() => { let it = 0; const max = 18; const iv = setInterval(() => { if (it >= max) { setD(target); clearInterval(iv); return }; setD(target.split("").map((c, i) => { if (!/[0-9]/.test(c)) return c; if (it > max - 5 && i < it - (max - 5)) return c; return String(Math.floor(Math.random() * 10)) }).join("")); it++ }, 50); return () => clearInterval(iv) }, delay); return () => clearTimeout(t) }, [target, delay]); return (<div className="flex flex-col gap-1"><span className="text-4xl lg:text-5xl font-mono font-bold tracking-tight text-foreground" style={{ fontVariantNumeric: "tabular-nums" }}>{d}</span><span className="text-[10px] tracking-[0.2em] uppercase text-muted-foreground">{label}</span></div>) }
export function MetricsCard() {
  return (<div className="flex flex-col h-full"><div className="flex items-center justify-between border-b-2 border-foreground px-4 py-2"><span className="text-[10px] tracking-widest text-muted-foreground uppercase">legalidad.ambitos</span><span className="inline-block h-2 w-2 bg-[#ea580c]"/></div><div className="flex-1 flex flex-col justify-center gap-6 p-6"><SN target="8" label="Ambitos Art. 2.40 EUDR" delay={400}/><SN target="4/8" label="Verificados: SUNARP,SERFOR,SENASA,SUNAT" delay={700}/><SN target="5" label="Factores de scoring v1.1" delay={1000}/><SN target="2026" label="Plazo PYMES · 30 junio" delay={1300}/></div></div>)
}
