"use client"
import { motion } from "framer-motion"
const P = ["MIDAGRI","SERFOR","SENASA","GeoBosques","PROMPERU","SUNARP","SUNAT","MINAM","UE","Peru"]
function LB({ name, glitch }: { name: string; glitch: boolean }) { return (<div className={`flex items-center justify-center px-8 py-4 border-r-2 border-foreground shrink-0 ${glitch?"animate-glitch":""}`}><span className="text-sm font-mono tracking-[0.15em] uppercase text-foreground whitespace-nowrap">{name}</span></div>) }
export function GlitchMarquee() { return (<section className="w-full py-16 px-6 lg:px-12"><div className="flex items-center gap-4 mb-8"><span className="text-[10px] tracking-[0.2em] uppercase text-muted-foreground">{"// ENTIDADES: ECOSISTEMA_PERU"}</span><div className="flex-1 border-t border-border"/></div><div className="overflow-hidden border-2 border-foreground"><div className="flex animate-marquee" style={{width:"max-content"}}>{[...P,...P].map((n,i)=>(<LB key={`${n}-${i}`} name={n} glitch={i%10===3||i%10===8}/>))}</div></div></section>) }
